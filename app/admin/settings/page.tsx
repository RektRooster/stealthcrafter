import ModuleStub from "../module-stub";

export default function SettingsPage() {
  return (
    <ModuleStub
      title="SETTINGS"
      icon="settings"
      desc="Command Center configuration — access control, data connections and console preferences."
    />
  );
}
